errors:
  unexpected_value: "{{ key }} 为非预期的值: {{ value }}"
  key_missing_in_path: "{{ path }} 不匹配, 在 {{ missing }} 中未找到"
  type_of_path_is_unexpected: "{{ path }} 为非预期的类型: 预期: {{ should }}, 实际: {{ actually }}"
  args_is_different: "{{ path }} 的参数的类型不同: [{{ one }}], [{{ two }}]"
